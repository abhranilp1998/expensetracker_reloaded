// TODO: Move the `_ActionCard` implementation out of `lib/home_dashboard.dart`
// and expose a public `ActionCard` widget here. Currently the implementation
// is a private class inside `home_dashboard.dart` (`_ActionCard`).
//
// Example export (after moving):
// export 'package:expensetracker_reloaded/widgets/action_card_impl.dart' show ActionCard;

// Placeholder to make the folder structure explicit.
